#spring.application.name=SpringBoot_SpringBatch

spring:
  datasource:
    url: jdbc:h2:mem:student_db
    username: admin
    password: admin
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  h2:
    console:
      enabled=true:
  batch:
    jdbc:
      initialize-schema: ALWAYS
    job:
      enabled: false
#spring.batch.jdbc.initialize-schema = always



